SECRET_KEY = "supersecretkey"

DATABASE = "./farmer.db"
SQLALCHEMY_DATABASE_URI = "sqlite:///farmer.db"
SQLALCHEMY_TRACK_MODIFICATIONS = false

JSON_AS_ASCII = false

JWT_ISSUER = 'farmer.fermedelacourt.be'

DEFAULT_USER = 'admin'
DEFAULT_PASS='admin!'

CORS_ORIGIN = "*"
CORS_HEADERS = [
  "Authorization",
  "Accept",
  "Accept-Language",
  "Content-Language",
  "Origin",
  "Content-Type",
]
CORS_METHODS = ["GET", "OPTIONS", "PUT", "POST", "DELETE"]
